// test/commands/registry/download.test.ts
import { expect } from 'chai';

describe('registry download (mock)', () => {
  it('should pass a dummy test', () => {
    expect(true).to.equal(true);
  });
});
